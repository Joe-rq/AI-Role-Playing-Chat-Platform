# 依赖 (所有目录)
node_modules/
# 警告：不要忽略 lock 文件！
# yarn.lock  <- 删除了，除非确定不需要它
# package-lock.json <- 确保这个文件是被 git 追踪的

# 环境与数据库
.env
*.env.local
*.sqlite
*.db

# 编译产物
dist/
build/
.cache/

# 运行时上传目录 (保留目录，忽略文件)
backend/uploads/*
!backend/uploads/.gitkeep

# 系统文件
.DS_Store
Thumbs.db

# IDE 和编辑器
.vscode/
.idea/
*.swp
*.swo

# 日志和临时文件
*.log
*.tmp
backend/dist/tsconfig.build.tsbuildinfo